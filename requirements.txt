jupyterlab          # Jupyter interface for running the notebooks
ipykernel           # Required for Jupyter to detect this environment
opencv-python-headless  # Maps to -> import cv2 (no GUI/X11 libs needed; no cv2.imshow use in this repo)
numpy               # Maps to -> import numpy
matplotlib          # Maps to -> import matplotlib
scikit-learn        # Maps to -> import sklearn
pandas
pillow              # Maps to -> import PIL
scikit-image        # Maps to -> import skimage (HOG features)
kagglehub           # Maps to -> import kagglehub (dataset download)
seaborn             # Maps to -> import seaborn (SVM classifier plots)
